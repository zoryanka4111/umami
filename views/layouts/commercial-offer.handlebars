<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0,  user-scalable=0, minimal-ui">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{title}}</title>
    <!-- open graph -->
    <meta property="og:url" content="{{serverUrl}}" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="{{title}}" />
    <meta property="og:description" content="{{description}}" />
    <meta property="og:image" content="{{serverUrl}}img/logoPNG.png" />
    <!-- open graph -->
    <!-- favicon -->
    <link rel="apple-touch-icon" sizes="180x180" href="{{puthSRC}}/src/www/img/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="{{puthSRC}}/src/www/img/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="{{puthSRC}}/src/www/img/favicon/favicon-16x16.png">
    <!-- favicon -->
    <link rel="stylesheet" href="{{puthSRC}}/dist/css/cssLibs{{version}}.css">
    <link rel="stylesheet" href="{{puthSRC}}/dist/css/bundle{{version}}.css">
</head>

<body>
    <div class="wrapper">
        {{>headerOffer}}
        {{{body}}}
        {{>footer}}
        {{!-- {{>popup}} --}}
    </div>

    <script src="{{puthSRC}}/dist/js/libs{{version}}.js" defer></script>
    <script src="{{puthSRC}}/dist/js/script{{version}}.js" defer></script>

</body>

</html>